---
- name: create and run ec2 instance
  hosts: localhost
  connection: local
  gather_facts: no
  roles:
    - aws-get
  tasks:

#=================== GATHER EC2 INFO ========================= TAG: ec2-info ================
#    - name: get ec2 info
      
